import React, { useState } from "react";

const HomePage = () => {
  return (
    <div className="container mt-4 text-center">
      <h1 className="mb-4">Image Editor Utility:</h1>
      <p className="mb-5">
        This application is meant for utility editing for web developers and designers. 
        Each section includes a specific tool that has set parameters to be used with 
        serverless functions and API calls. 
      </p>
    </div>
  );
};

export default HomePage;
